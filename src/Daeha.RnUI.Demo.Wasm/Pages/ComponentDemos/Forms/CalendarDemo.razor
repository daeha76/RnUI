@using Daeha.RnUI.Demo.Wasm.Models

@code {
    public static ComponentDemo GetDemo() => new(
        "Calendar", "calendar", "A date field component that allows users to enter and edit date.",
        Demo: @<div class="flex items-start gap-8">
                <div>
                    <p class="text-sm text-muted-foreground mb-2">Default</p>
                    <RnCalendar />
                </div>
                <div>
                    <p class="text-sm text-muted-foreground mb-2">Small</p>
                    <RnCalendar Size="ComponentSize.Sm" />
                </div>
                 <div>
                    <p class="text-sm text-muted-foreground mb-2">Extra Small</p>
                    <RnCalendar Size="ComponentSize.Xs" />
                </div>
            </div>,
        Thumbnail: @<div class="text-xs text-muted-foreground">Calendar</div>,
        Code: @"<RnCalendar @bind-Selected=""_date"" />

@* Small size — ideal for sidebars *@
<RnCalendar @bind-Selected=""_date"" Size=""ComponentSize.Sm"" />

@* Extra Small size *@
<RnCalendar @bind-Selected=""_date"" Size=""ComponentSize.Xs"" />

@code {
    private DateTime? _date;
}",
        UsageCode: @"@using RnUI.Components.UI.Calendar

<RnCalendar @bind-Selected=""_date"" />

@* Small size — ideal for sidebars *@
<RnCalendar @bind-Selected=""_date"" Size=""ComponentSize.Sm"" />

@* Extra Small size *@
<RnCalendar @bind-Selected=""_date"" Size=""ComponentSize.Xs"" />

@code {
    private DateTime? _date;
}"
    );
}
