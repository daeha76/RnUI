@if (IsExpanded)
{
    <div data-slot="accordion-content" class="cn-accordion-content overflow-hidden">
        <div class="@CssUtils.Cn("cn-accordion-content-inner", Class)" @attributes="AdditionalAttributes">
            @ChildContent
        </div>
    </div>
}

@code {
    [CascadingParameter] public RnAccordion? AccordionParent { get; set; }
    [CascadingParameter] public RnAccordionItem? ItemParent { get; set; }
    [Parameter] public RenderFragment? ChildContent { get; set; }
    [Parameter] public string? Class { get; set; }
    [Parameter(CaptureUnmatchedValues = true)] public Dictionary<string, object>? AdditionalAttributes { get; set; }

    private bool IsExpanded => AccordionParent?.IsExpanded(ItemParent?.Value ?? "") ?? false;
}
